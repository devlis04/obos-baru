name: admin
description: Obos admin
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  supabase_flutter: ^2.17.2
  flutter_map: ^8.3.2
  latlong2: ^0.10.1
  web: ^1.1.1
  obos_core:
    path: ../../packages/obos_core
  obos_auth:
    path: ../../packages/obos_auth

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
    - assets/icon/app_icon_white.png
    - assets/icon/app_icon.png
